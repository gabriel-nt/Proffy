import styled from 'styled-components';

export const SwitchContainer = styled('div')`
    position: absolute;
    top: 2rem;
    right: 2rem;
    color: transparent;

    @media(min-width: 700px) {
        top: 4rem;
        right: 4rem;
    }

`;
